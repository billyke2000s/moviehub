{
  "name": "moviehub",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "deploy": "npm --prefix cloudflare run deploy",
    "dev": "npm --prefix cloudflare run dev",
    "test": "node --check cloudflare/src/index.js && python3 tools/validate.py"
  }
}
